# Mermaid Preview Test

If this chart renders in Markdown Preview, Mermaid support is working.

```mermaid
flowchart LR
  A[Input] --> B[Controller]
  B --> C[Plant]
  C --> D[Output]
  D -->|feedback| B
```
